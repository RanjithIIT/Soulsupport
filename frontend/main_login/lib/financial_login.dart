import 'package:flutter/material.dart';

class FinancialLoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Financial Login'),
      ),
      body: Center(
        child: Text('Financial Login Page - Coming Soon'),
      ),
    );
  }
}
