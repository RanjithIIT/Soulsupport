import 'package:flutter/material.dart';

class FinancialProfilesPage extends StatelessWidget {
  const FinancialProfilesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Financial Profiles'),
      ),
      body: const Center(
        child: Text('Financial Profiles Page - Coming Soon'),
      ),
    );
  }
}
