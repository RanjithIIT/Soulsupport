import 'package:flutter/material.dart';

class RTIActPage extends StatelessWidget {
  const RTIActPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RTI Act'),
      ),
      body: const Center(
        child: Text('RTI Act Page - Coming Soon'),
      ),
    );
  }
}
